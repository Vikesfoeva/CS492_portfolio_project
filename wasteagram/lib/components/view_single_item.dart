import 'package:flutter/material.dart';

class WasteListItem extends StatelessWidget {
  const WasteListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
